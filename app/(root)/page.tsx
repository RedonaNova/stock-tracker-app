import { Button } from "@/components/ui/button";
import Image from "next/image";

/**
 * Renders the Home page wrapper containing the text "Home".
 *
 * @returns A JSX element: a div with classes "flex min-h-screen home-wrapper" that displays the text "Home".
 */
export default function Home() {
  return <div className="flex min-h-screen home-wrapper">Home</div>;
}